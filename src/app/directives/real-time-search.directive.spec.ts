import { RealTimeSearchDirective } from './real-time-search.directive';

describe('RealTimeSearchDirective', () => {
  it('should create an instance', () => {
    const directive = new RealTimeSearchDirective();
    expect(directive).toBeTruthy();
  });
});
